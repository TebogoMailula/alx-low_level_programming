#include "main.h"
/**
 *_islower - checks for lowercase characters
 *@c: char to be checked
 *Return: 1 if lowercase or else 0
 */
int _islower(int c)
{
if (c >= 'a' && c >= 'a')
{
return (1);
}
else
{
return (0);
}
}
